<h2>Edit expense</h2>

<%= render "form.html", changeset: @changeset,
                        action: expense_path(@conn, :update, @expense) %>

<%= link "Back", to: account_path(@conn, :show, @expense.account_id) %>
